ParsingTime = 
VAR StartTime = MIN('secure_log'[time])  // Timestamp du début du parsing (première entrée)
VAR EndTime = MAX('secure_log'[time])    // Timestamp de la fin du parsing (dernière entrée)
RETURN
    DATEDIFF(StartTime, EndTime, SECOND)
